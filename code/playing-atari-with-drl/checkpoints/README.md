# Model weights (local only — not committed)

- `cartpole_baseline/` — shareable baseline (`model.pt`, config, RESULTS.md)
- Frozen reference: `baselines/cartpole_seed42/`

Generate via `python train.py` or copy from `outputs/<experiment>/seed_<n>/model_final.pt`.
